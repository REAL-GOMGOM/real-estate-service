# 실거래 정확성 개선 — 2026-09-06

## 이번 반영 범위

1. **검색·공유의 단지 식별 보존**
   - 전세·월세·분양권도 검증된 단지 ID로 서버에서 먼저 필터링한다.
   - 구별 상위 60개 목록 밖의 단지가 `0건`으로 사라지지 않는다.
   - 기존 이름 기반 URL은 동 정보까지 전달하며, 새 공유 URL에는 가능한 경우 단지 ID·거래 유형·계약 식별자를 보존한다.
   - 단지 조회는 계약 내역을 최근 10개로 자르지 않아 이전 공유 계약도 확인할 수 있다.
   - 기준 데이터 장애(503)와 확인된 단지 없음(404)을 구분한다.
   - URL의 기간·거래 유형이 바뀌면 새 요청에 해당하는 응답을 기다린 후 공유 계약을 연다. 이전 응답으로 새 링크를 열지 않는다.

2. **가격 비교 기준 통일**
   - 최신 표시 거래의 유사 면적(기존 기준 ±6㎡)과 조회 기간으로 카드·상세·공유 비교를 통일한다.
   - 과거 계약을 공유할 때 그 계약보다 미래의 거래를 비교에 사용하지 않는다.
   - 비교 부족·동가·하락·기간 최고가 경신을 구분한다. 거래 1건을 신고가로 간주하지 않는다.
   - 같은 날짜의 선후관계나 서로 다른 직전 가격은 임의로 추정하지 않는다.
   - 월 단위만 알려진 기록이 대상 계약보다 미래일 수 있으면 비교에서 제외한다.
   - 상세 표의 비율도 계약 당시 유사 면적 기준이며, 차트에 다른 면적 가격을 섞지 않는다.
   - 카드의 거래 수 설명에는 실제 조회 기간을 표시한다.

## 검증

- `npm run test:run`: 183개 파일, 1,709개 테스트 통과.
- `npm run lint`, `npx tsc --noEmit`, `git diff --check` 통과.
- `npm run build`: 프로덕션 빌드 성공. 로컬에는 공개 블로그 원본 설정이 없어 블로그/RSS/sitemap의 기존 실패 안전 처리 경고가 발생했다. 배포는 이 로컬 산출물을 업로드하지 말고 Vercel의 운영 설정으로 다시 빌드한다.
- 실제 공개 스냅샷을 이용한 로컬 화면 검증:
  - 강남구 일원동 `가람` 전세: 2건(9억, 8.8억) 표시.
  - `잠실엘스` 85㎡: 34.1억과 종전 33.5억 비교. 60㎡ 최고 30.05억을 기준으로 사용하지 않음.
  - 신규 브라우저 세션에서 관련 화면 콘솔 오류 없음.
- 회귀 테스트: 모든 거래 유형의 ID/별칭 검색, 공유 URL, 오래된 계약, 기간 변경 중 응답 대기, 단일 거래, 동가, 같은 날짜 거래, 유사 면적 비교.
- 실제 Windows OS는 이 작업 환경에 없어 직접 검증하지 못했다. 기존 이미지 다운로드 폴백의 자동 테스트는 통과했다.

## 운영 반영 상태

- 2026-09-06 운영 반영 완료. 배포 커밋 `ae8e839`, Next.js 16.2.11, 상태 `READY`.
- 운영 URL: [www.naezipkorea.com](https://www.naezipkorea.com/).
- 검증 후 승격한 배포: [jwm05v6ia](https://real-estate-service-jwm05v6ia-real-gomgoms-projects.vercel.app/), ID `dpl_CSeGtkM9GBkMFindgBjFpwkYcDww`.
- Vercel 운영 설정으로 원격 빌드(약 1분) → `--skip-domain` 검증 → `vercel promote` 순서로 반영했다. 검증 전까지 기존 운영 도메인이 이전 배포를 가리키는 것도 확인했다.
- 이전 운영 배포(복귀 기준): `dpl_9soGUjSydLQG9ZuKnQEKm9SUeaui`.
- 이전에 커밋된 평·억 단위 가격 제보 폼 및 데스크톱 이미지 다운로드 개선도 함께 반영됐다.

### 배포 검증 결과

- 검증용 배포의 인증된 GET: 가람 전세 2건, 월세 2건, 이름·동 기반 기존 URL, 잠실엘스 5건, 존재하지 않는 단지 ID의 404, 제보 접수 활성 상태 통과.
- 검증용 브라우저: 홈 검색 → 일원동 가람 선택 → 전세 탭 2건 렌더 확인. 공유 URL → 잠실엘스 85㎡ 34.1억 계약 자동 열림 및 종전 33.5억 비교 확인.
- 제보 폼: 25.7평 → 전용 84.96㎡, 12.5억 → 12억 5,000만원 안내 확인. 익명·현장소식 옵션 확인. 실제 제보 POST는 하지 않았다.
- 운영 승격 후 로그인 없는 공개 GET에서 가람 2건·잠실엘스 5건·제보 접수 활성 상태 재확인. 운영 홈페이지에서도 새 제보 폼과 기존 메인 문구 확인, 브라우저 콘솔 오류 없음.
- 새 배포의 최근 1시간 `error`/`fatal` 로그 조회 결과 없음. 외부 로그 전송 Drains는 0개이며, 기존 예약 모니터의 실행 상태는 이번 배포에서 다시 검사하거나 변경하지 않았다.
- 빌드 중 블로그 스냅샷 manifest 요청 경고가 있었다. 기존 운영과 새 배포의 `/blog`, `/api/blog/feed`, `/blog/rss.xml`은 모두 200이며 같은 빈 목록 상태였다. 이번 수정으로 생긴 차이는 아니지만 원본 제공 상태는 별도 점검 대상이다.

### 추가로 확인한 보완 사항

- 이름·동 기반 전월세 조회에서 거래 목록은 가람 2건이 맞지만 API의 `total`은 이름 필터 전 일원동 전체 37건을 반환한다. 현재 화면은 단지별 `txCount`를 합산하므로 정확히 2건을 표시한다.
- 전월세·분양권 스냅샷/실시간 경로의 합계는 이름 필터 후, 목록 제한 전에 계산하도록 후속 보완한다. 같은 동의 다른 단지·목록 제한 회귀 테스트도 필요하다.

## 다음 순위

1. 검색 API의 이름 필터 합계 메타데이터 보정 및 방문 날짜 대신 실제 데이터 수집 시각 표시.
2. 메인 빈 제보 영역 축소, 제보에서 해당 단지 실거래로 연결, 최근 거래·관심 단지 접근 개선.
3. 3개월~3년 조회의 부분 실패 처리와 과거 데이터 제공 안정화.

메인 기존 소개 문구, 제보의 미확인 정보 구분, 개인정보 보호·검수 절차는 유지한다.
