# 마이집 핸드오버 2026-08-02

> 이전: HANDOVER-2026-07-26 → 이번 세션(8/1~8/2, Cowork): Neon 복구 + 집계 푸시다운 전환 + 월초 공백 해소
> **이 문서는 Claude Code 세션 인수인계용 — "남은 작업"부터 순서대로 진행할 것.**

## 이번 세션 완료

### 1. Neon 402 복구 (8/1)
- 8/1 00:00 UTC 전송량 리셋 확인 → `TRANSACTIONS_SOURCE=db` 복원(sensitive 타입, 동작 무관) + 재배포
- db-usage: 431.4MB/512MB, 쓰기 OK · 봇 /draft 정상 · 실거래 API 정상
- **백필 완료**: 장애 기간(7/26~8/1) 공개분 — 매매 33,206 + 전월세 55,507 upsert, 실패 0

### 2. 집계 3종 SQL 푸시다운 전환 (8/2) — 전송량 사고 재발 방지 본진
- 신규: `lib/agg-window.ts`(윈도우 해석, KST) + `lib/agg-queries.ts`(푸시다운 쿼리)
- 전환: `app/api/transactions/{summary,districts,highlights}/route.ts` — 원장 풀전송(콜당 2.8만~3.3만 행) → 집계 결과만(~250행, **99% 절감**). `?window=rolling30(기본)|YYYYMM`
- **버그 픽스 (districts)**: 거래 1건 단지도 신고가로 세던 옛 로직 제거 — countNewHighs 의미론으로 통일. 구 칩 신고가 수치 하락은 정상
- **버그 픽스 (반올림)**: PG `round(float)`=은행가 반올림 ↔ JS `Math.round` 불일치 → `round(area_m2::numeric)`으로 통일
- **의미론 개선(의도)**: 같은 날 복수 거래 시 그날 최고가 기준 신고가 판정(결정적) — 기존은 행 순서 의존 비결정적. 신고가 수치 소폭 상향은 정상
- 검증: `scripts/verify-agg-parity.ts` — cnt·avg 완전 일치(rolling30 28,138 / 202607 32,569), 신고가 차이는 전부 old≤new 단방향 확인됨. vitest 732개 통과(agg-window 14케이스 포함)

### 3. sync 크론 — 전월 지연신고 영구 누락 픽스
- `SYNC_MONTHS 1→2` (신고기한 30일 구조상 전월 계약이 익월 내내 공개됨 — 당월만 돌리면 전월 원장 동결)
- 매매 페이즈에도 일별 시작점 로테이션 추가 (잡 2배 → 예산 초과 시 스킵 꼬리 회전)

### 4. 화면 — 월초 공백 해소 + 지난달 조회
- `/transactions` 메인: [오늘 공개 | 최근 30일 | 8월 | 7월] 토글. '오늘 공개'=봇 집계분만(없으면 0건, 봇 데이터 없는 날 탭 자동 숨김+rolling30 전환)
- `/highlights`: 서버 기본 rolling30 + 라벨 정합
- 3층 구조: 시/도 카드(탭이 기간 결정) → 구 선택 모달(칩=30일) → 구 상세(자체 기간 3개월~3년, 기본 6개월)

## 남은 작업 (우선순위 순)

1. **[즉시] 재배포** — '오늘 공개' 탭 정합 픽스(TransactionsClient 3곳: 폴백 혼합 제거, 오늘 데이터 없으면 0건) 코드만 완료, **미배포 상태**. `npx vercel --prod` → 헤더 총건수와 카드 합이 일치하는지 확인
2. **[즉시] 커밋 정리** — 오늘 변경분 전부 미커밋:
   - 신규: `lib/agg-window.ts`, `lib/agg-queries.ts`, `lib/__tests__/agg-window.test.ts`, `scripts/verify-agg-parity.ts`, 이 문서
   - 수정: 집계 라우트 3종, `TransactionsClient.tsx`, `HighlightsClient.tsx`, `app/api/cron/sync-transactions/route.ts`
   - 주의: `components/location-map/SchoolDetailPanel.tsx`는 G 직접 수정분(7월부터 미커밋) — **분리 커밋, 내용 G 확인 후**
3. **[내일] 크론 모니터링** — 새벽 5시 sync 로그의 `s=`(스킵) 확인. 로테이션으로 하루 이틀 내 수렴해야 정상, 지속적으로 크면 예산 조정
4. **[이번 주] 애드센스 재도전 준비** — "가치가 별로 없는 콘텐츠" 거절(심사 기간이 장애 기간과 겹침). CSR 페이지(highlights·transactions 등)에 서버 렌더 설명 텍스트 보강 + 칼럼 주 2~3개 발행 → **1~2주 뒤** 재검토 요청. 지금 누르지 말 것
5. **[선택]** `transactions(deal_date)` 단독 인덱스(현재 윈도우 스캔은 seq scan ~수십ms — 급하지 않음) · 구 칩 기간을 상세 기간과 연동 · 맥미니 사전집계 배치(푸시다운으로 시급성 소멸 — 백로그)

## 환경 메모
- Vercel prod `TRANSACTIONS_SOURCE=db` (sensitive — 대시보드에서 값 안 보이는 게 정상)
- Neon: 스토리지 431/512MB (84%) — 원장 증가 추세 주시, Usage 탭 주 1회. 보존 정책: 매매 13개월/전월세 7개월
- 쿠팡 최종 승인 대기(스크린샷 제출), KESS 파싱 등 7/26 문서의 미결 항목 유지

## 오후 세션 추가분 (8/2 오후, Cowork)

### 완료
1. **남은 작업 1·2번 완료** — 재배포(정합 검증 통과: rolling30 27,050건 = 헤더) + 분리 커밋 5개. `SchoolDetailPanel.tsx`는 여전히 미커밋 (G 확인 대기)
2. **전세·월세 시/도 집계** (`c5b0f58`) — summary `?dealType=jeonse|monthly`, `fetchRentDistrictAggs` 푸시다운. 카드=평균 보증금(월세는 /평균 월세 병기), 신고가·오늘공개는 매매 전용. 검증 `scripts/verify-rent-agg.ts`
3. **분양권 탭** (`520764d`) — 매매 숫자 카드 오인 문제 → 지역 선택 전용 그리드로 교체 (DB 원장 없음)
4. **맥미니 원장 미러 — Neon 의존도 완화 1단계** (`ce71243`)
   - 로컬 Postgres 17 (brew, DB `naezip`) + 초기 백필 완료 (매매 529,826 · 전월세 440,137 · 단지 29,334 — Neon 과 일치)
   - `scripts/macmini-mirror.sh`: updated_at 하이워터마크 증분, DELETE 미전파 (Neon 이 지워도 로컬 보존)
   - launchd `com.gomgom.naezip-mirror` 매일 06:30, 로그 `~/Library/Logs/naezip-mirror.log`

### 남은 작업 추가
- **[선택] 의존도 완화 2단계** — 수집(MOLIT sync)을 맥미니로 이전 → Neon 에 최근분+집계만 push, Neon 보존 단축(스토리지 84%→여유). 1단계 미러가 안정 돌아간 뒤(1주 관찰) 진행 권장
- **[선택] 분양권 원장** — silv 도 DB 적재하면 시/도 집계 가능 (거래량 적어 부담 소)

### 2단계도 당일 완료 — MOLIT 수집 맥미니 이전
- `scripts/macmini-sync.ts` — Vercel sync 크론 대체. 로컬(naezip)+Neon 이중 upsert, 55s 예산·스킵 없이 전 구(248)×2개월 매일 완주 (검증 131s, 실패 0, local=neon 카운터 일치)
- launchd `com.gomgom.naezip-sync` 매일 05:00 KST · `com.gomgom.naezip-mirror` 06:30(안전망 유지)
- `vercel.json` 크론 제거·배포 — `/api/cron/sync-transactions` 라우트는 수동 폴백용 유지
- Neon 보존 정리는 sync 스크립트가 동일 수행 (매매 13개월/전월세 7개월)
- **내일(8/3) 모니터링**: `~/Library/Logs/naezip-sync.log` 05:00 실행 확인 (기존 Vercel 크론 로그 대신). 스킵 개념은 사라짐 — fetch/local 실패 0 인지, Neon f= 값만 보면 됨
- 함정 메모: `.env.local` 의 Neon `PGUSER/PGPASSWORD` 가 pg 드라이버 빈 필드를 채움 — 로컬 연결 문자열에 사용자 명시 필수 (스크립트 반영됨)

### 옵시디언 글창고 (8/2 오후 추가)
- iCloud Drive `Obsidian/글창고` — 내집 칼럼 43편(MDX 원문+frontmatter) + 뉴스 아카이브 111일치. 맥북 옵시디언과 자동 동기화, Obsidian 앱 맥미니에도 설치됨
- `scripts/export-obsidian.ts` + launchd `com.gomgom.obsidian-export` 매일 07:00 (멱등 전체 재작성)
- 미결: 텔레그램 발송 원문 아카이빙(봇 send_message 저장 훅 — 가동 중 봇이라 신중히)
