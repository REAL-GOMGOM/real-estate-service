export default function MarketLoading() {
  return (
    <div style={{ minHeight: '60vh', display: 'flex', alignItems: 'center', justifyContent: 'center', paddingTop: 64 }}>
      <p style={{ color: 'var(--text-dim)', fontSize: 14 }}>불러오는 중...</p>
    </div>
  );
}
